version: 2

seeds:
  - name: employees
    description: a manual map of employees to customers
    columns:
      - name: employee_id
        description: Primary key of employees data
        tests:
          - unique
          - not_null